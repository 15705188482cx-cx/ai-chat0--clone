/**
 * useFirstLaunch 测试
 */
import { renderHook, act } from '@testing-library/react-hooks';
import { useFirstLaunch } from '../src/hooks/useFirstLaunch';

// 这部分测试需要 @testing-library/react-hooks,但项目没有装。
// 放个 skip describe 做占位,后续补充。
describe.skip('useFirstLaunch', () => {
  it('placeholder - needs @testing-library/react-hooks', () => {});
});
